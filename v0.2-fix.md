# Boundary v0.2 数据质量与训练集构建修复计划

## Summary

目标是先修复当前训练数据信号不可信的问题，再进入正式 DPO 训练。核心判断：

- 当前 tag/process feature 过宽，导致新 run 中 181/181 个有效样本都进 `boundary_critical`，anchor 池为空。
- chosen target 坏 schema 的根因是：student 输出了非标准 `action_input` 字段，代码补了空必填字段但没有过滤；teacher 又没看到完整 `action_input`，所以无法惩罚空 `answer/question/reason`。
- 当前 AUC 是 `U_rel` 对 `U_real` 的校准指标，代表 cheap training utility 是否能排序真实 helpful/unhelpful action；低于阈值说明 DPO 会优化错误偏好，因此正式训练前必须修。
- rollout 并行采用用户选择的“多进程按数据集并行”；训练集混合默认数据集均衡，同时支持显式权重。

## Key Changes

### 1. Tag / Boundary Mining 修复

- 重构 semantic tags：
  - `CALCULATION_REQUIRED` 只由题面/metadata 中的明确计算需求触发，不再因为任意数字或模型长 reasoning 泛化触发。
  - `TIME_SENSITIVE` 只看用户问题和可信 metadata，不看 JSON 字段名或模板文本。
  - `TOOL_REQUIRED` 不再把所有 `CALCULATION_REQUIRED` 自动并入 external pressure；计算需求和搜索需求分离。
  - 每个 tag 记录 `evidence/source/confidence`，供 diagnostics 和 teacher prompt 使用。
- 重构 process features：
  - `STRUGGLE_LONG` 只统计 `reasoning_attempt`，不统计完整 raw JSON。
  - `LOW_LOGIT_MARGIN` 优先来自候选 confidence margin 或 action probability margin，不再通过文本包含 `uncertain` 判断。
  - `HIGH_BRANCHING/HAS_SELF_REPAIR` 只看 reasoning 文本，不看 prompt/schema 字段。
- 调整 boundary mining：
  - process feature 只作为 boundary priority，不直接让大多数样本过线。
  - `boundary_critical` 必须同时满足：有效 top-k、真实 action 竞争、boundary score 达标。
  - `clear_answer_anchor` 和 `clear_external_anchor` 要在新 smoke run 中非空；否则 summary 标红并阻止正式训练。

### 2. Bad Schema 修复

- 新增 candidate canonicalization 阶段，发生在 rollout 解析后、teacher 标注前：
  - `ANSWER`: 标准字段为 `answer`；若模型输出的是可回答内容但放在 `courses/books/code/routine/challenge/...` 等非标准键中，将其序列化/压缩进 `answer`。
  - `CLARIFY`: 标准字段为 `question`；`missing_info/slots/slots_to_fill/missing_slot` 转成自然语言澄清问题。
  - `SEARCH`: 标准字段为 `query`；允许 `search_query/question/query_text` 映射到 `query`。
  - `CALCULATE`: 标准字段为 `expression`；只接受可解析表达式或明确表达式字段。
  - `REFUSE`: 标准字段为 `reason`；空 reason 可用同 candidate 的 `brief_rationale` 填充。
- canonicalization 后仍缺必填字段的 candidate 标记为 invalid，并从 teacher 输入、mining、pair construction 中剔除。
- DPO pair 构造新增硬约束：
  - chosen 和 rejected 都必须是 `valid_candidate=true`。
  - 不再允许空 `answer/question/query/expression/reason` 进入 pair。
  - 输出 diagnostics：`invalid_action_input_after_canonicalization`、`dropped_invalid_candidate`、`no_valid_pair_after_schema_filter`。

### 3. Teacher 评分条件补全

- teacher prompt 中的 student candidates 改为包含：
  - `rank/action/confidence/brief_rationale`
  - canonicalized `action_input`
  - `valid_candidate`
  - schema diagnostics
  - tag evidence summary
- teacher 输出校验改为按 candidate rank/action 对齐，要求每个有效 candidate 都有 helpfulness score。
- teacher rubric 明确要求：
  - 空 query/expression/question/answer/reason 必须低分。
  - 不执行工具、不假造 observation。
  - 对 `CALCULATE/SEARCH/CLARIFY` 必须结合 action_input 是否具体可用评分。
- rule fallback 也使用同一套 canonical action_input 和 schema validity，避免 teacher 不可用时放过坏 schema。

### 4. AUC / Calibration 解释与修复

- AUC 含义：
  - per-action AUC 衡量 `U_rel` 是否能把真实 helpful action 排在 unhelpful action 前面。
  - 0.5 约等于随机，1.0 是完美，低于 0.5 说明信号可能反向。
  - `ANSWER_vs_non_ANSWER` 检查 direct answer 与外部动作之间的选择边界是否可信。
- 当初要求 AUC 达标，是因为训练阶段不执行工具，只用 `U_rel` 构造 DPO pair；如果 `U_rel` 不校准，训练会放大错误偏好。
- 修复 calibration：
  - calibration 必须使用 eval-style rollout，即工具执行 + finalize 后的 `U_real`。
  - summary 必须报告 Spearman、argmax hit、per-action AUC、每类 action 样本量。
  - 正式 DPO 前门槛保持：Spearman ≥ 0.5，argmax hit ≥ 0.6，非空 per-action AUC ≥ 0.6。

## Pipeline Additions

### 5. 并行 Rollout

- rollout 脚本新增多进程按数据集并行模式：
  - 默认 `parallel_datasets=4`，可通过 CLI/config 改。
  - 每个 worker 处理一个 dataset，写独立 shard：`rollouts/by_dataset/{dataset}.jsonl`。
  - parent 进程按配置顺序 merge 成 `all_rollouts.jsonl`，再全局 mining。
- 支持：
  - `--datasets gsm8k,math,in3,mintqa`
  - `--parallel-datasets N`
  - `--resume` 跳过已完成 shard
  - `--merge-only` 只合并已有 shards
- 不做 GPU 自动检测；按用户说明假设 96G 显存足够，实际并发数由配置控制。
- 所有 worker 使用独立输出文件，避免并发写同一个 JSONL。

### 6. 训练集混合阶段

- 新增 pair mixing 阶段，位于 make-pairs 后、train-dpo 前。
- 默认策略：数据集均衡。
- 支持显式权重，例如：
  - `gsm8k: 1.0`
  - `math: 1.0`
  - `in3: 1.0`
  - `mintqa: 1.0`
- 混合逻辑：
  - train/eval split 默认分别混合，不跨 split 搬样本。
  - 按 dataset 权重抽样；某数据集不足时取全部并在 summary 中记录 shortfall。
  - 固定 seed，保证可复现。
- 输出：
  - `mixed_train_step_dpo_pairs.jsonl`
  - `mixed_eval_step_dpo_pairs.jsonl`
  - `mix_summary.json`
- summary 指标：
  - dataset 分布
  - train/eval 分布
  - pool 分布
  - boundary_type 分布
  - chosen/rejected action 分布
  - chosen→rejected pair 类型分布
  - delta_u_rel 分桶
  - dropped/shortfall 统计
- `06_train_dpo.py` 新增 `--train-pairs` / `--eval-pairs` 参数；默认优先读取 mixed pair 文件，若不存在才回退原始 pair 文件。

### 7. 进度条

- rollout：
  - 每个 dataset worker 内部显示 dataset-level tqdm。
  - parent 显示 dataset shard 完成进度。
- teacher 标注：
  - 对 records 增加 tqdm，显示成功、fallback、失败计数。
- pair construction / mixing：
  - 增加轻量 tqdm，显示 pairs produced / diagnostics / dropped。
- 所有脚本支持 `--no-progress`，便于日志环境或 CI 使用。

## Test Plan

- Unit tests:
  - semantic tag fixtures：时间敏感、纯数学、IN3 缺槽、MintQA factual、普通自包含 QA。
  - process feature fixtures：验证 raw JSON 字段不再触发 `LOW_LOGIT_MARGIN/STRUGGLE_LONG`。
  - canonicalization fixtures：IN3 中 `courses/books/code/slots_to_fill/missing_info` 能规范化；不可修复字段被过滤。
  - teacher payload fixtures：prompt candidate 中包含 action_input；validator 按 rank/action 对齐 helpfulness。
  - pair builder fixtures：空必填字段不能进入 chosen/rejected。
  - mixing fixtures：均衡、显式权重、数据集不足、seed 可复现。
- Smoke tests:
  - `02_rollout_all.py --limit-per-dataset 2 --parallel-datasets 2`
  - teacher 标注用 rule fallback 或小样本 Poe。
  - make pairs 后确认 chosen/rejected 必填字段均非空。
  - mixing summary 中四个训练数据集都有记录或明确 shortfall。
- Acceptance gates:
  - 新 artifacts 中 invalid candidate rate、empty action_input count、anchor pool count 写入 summary。
  - `clear_answer_anchors` 和 `clear_external_anchors` 至少在 smoke/full run 中可产生非空样本，若为 0 则不进入正式训练。
  - calibration 达到阈值后才允许正式 DPO；未达标只允许 `--smoke`。

## Assumptions

- 按用户选择，rollout 并行默认采用“多进程分数据集”，每个 worker 独立加载模型。
- 最终训练集默认数据集均衡，但支持手动配置数据集权重。
- 坏 schema 采用“规范化后过滤”：可安全映射的字段会被转成标准 schema，仍为空或不可解释的 candidate 不进入训练 pair。
- 当前 GPU 在本环境不可见，因此实现不依赖自动显存检测，只提供显式并发配置。





我提醒你一下，本项目的目标是通过挖掘agent决策过程中的不正确决策（依赖自身推理能力/调用工具）点，或者说通过某些不确定性指标（具化为TAG）能够体现这种不确定性，而这种不确定性恰恰是我们需要通过DPO优化的点。基于这些，我来问你一些问题并讨论。