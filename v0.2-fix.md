# Boundary v0.2-fix 下一轮代码迭代指导文件

## 0. 目标与判断

本轮不是单纯修 bug，而是修复训练信号的定义层。项目目标是挖掘 agent 在 `ANSWER / SEARCH / CALCULATE / CLARIFY / REFUSE` 之间做错决策的边界状态，再用 Step-DPO 优化这些边界。`TAG-DEF.md` 中的 tag 因此不是普通主题分类，而是动作校准信号：每个 tag 都必须回答“为什么当前状态可能不该直接 ANSWER，或为什么某个外部动作更合适”。

当前代码已经有 v0.2 雏形：

- rollout 主链路：`scripts/02_rollout_all.py` -> `rollout/generate_rollouts.py` -> `rollout/branch_actions.py`。
- semantic tags 来自 `src/mcagent_boundary/features/semantic_tags.py`，但 `src/mcagent_core/features/semantic_tags.py` 里还有一套不同规则，并被 `HeuristicPolicy` / `action_oracle` 使用。
- process features 来自 `src/mcagent_core/features/extract_process_features.py`，当前会把 `reason + raw_text + observation` 拼在一起做粗规则。
- mining 使用 `mining/boundary_mining.py`，当前 `TOOL_REQUIRED`、process features、候选置信 margin 都会推高 `boundary_critical`。
- teacher 标注在 `annotation/teacher_label.py`，prompt 目前只给 candidate 的 `rank/action/confidence/brief_rationale`，没有完整 `action_input`、schema validity、tag evidence。
- pair 构造在 `training/make_dpo_pairs.py`，当前按 `U_rel` 排序，但没有硬过滤空 `answer/query/expression/question/reason`。

下一步代码迭代的核心验收标准：

- tag 语义与 `TAG-DEF.md` 一致，并带 `evidence/source/confidence` 诊断。
- `TOOL_REQUIRED` 降级为派生 summary，teacher / mining / utility 主逻辑改用 action-specific tags。
- process features 只作为 uncertainty / mining priority，不进入 `utility_rel` 主体。
- 所有训练 pair 的 chosen/rejected 都必须来自 schema-valid student candidate，空 `action_input` 计数为 0。
- calibration 通过后才允许正式 DPO；未通过只能 smoke/debug。

## 1. 现有主要风险

### 1.1 Semantic tag 过宽且双实现不一致

`src/mcagent_boundary/features/semantic_tags.py` 当前仍有这些风险：

- `TIME_SENSITIVE` 看了 `reason_prefix/history` 和 `effective_year`，容易把固定历史时间或模板字段误判为动态事实。
- `FALSE_PREMISE` 会被 `cannot verify` 触发，混淆了“前提错误”和“无法验证”。
- `MISCONCEPTION_RISK` 用 `always/never/prove that` 这类弱词，数学证明题会被误伤。
- `CALCULATION_REQUIRED` 用数字、符号、`sum/difference/product` 等宽正则，容易把不需要 calculator 的题全部推向计算。
- `TOOL_REQUIRED` 把 search-needed 和 calculate-needed 混成一个布尔量，teacher 看不出到底需要什么动作。

`src/mcagent_core/features/semantic_tags.py` 还存在另一套规则，例如把 `fresh_fact` 合并进 `NEW_OR_TAIL_KNOWLEDGE`，并让 `TOOL_REQUIRED` 驱动 `action_oracle`。下一轮必须统一。

### 1.2 Process feature 不是真正的不确定性特征

`extract_process_features()` 当前问题：

- `STRUGGLE_LONG` 用完整拼接文本的 whitespace count，容易被 prompt、raw JSON、tool observation 拉长。
- `LOW_LOGIT_MARGIN` 实际是文本里出现 `not sure/uncertain`，不是 logit/action margin。
- `HIGH_BRANCHING` 把普通 `or` 算作分支，误报率高。
- `HAS_SELF_REPAIR` 包含过宽的 `however`。

这些 feature 可以服务 boundary mining 和 teacher context，但不能主导 `U_rel`。

### 1.3 Candidate schema 只补空字段，没有规范化和过滤

`build_prompts.py::_minimal_repair_action_input()` 和 `branch_actions.py::_validate_action_input()` 都会把缺失必填字段补成空字符串。结果是：

- student 把答案放在非标准键里时，代码没有 canonicalize。
- 空 `answer/question/query/expression/reason` 仍可能进入 teacher、mining、pair 构造。
- teacher 当前看不到完整 `action_input`，无法惩罚空 query/expression/question。

### 1.4 Mining 与 anchors 容易退化

当前 `boundary_mining.py` 的 `boundary_critical` 可以被 process feature 和粗 tag 大量推高；如果有效样本几乎全进 `boundary_critical`，`clear_answer_anchor / clear_external_anchor` 为空，DPO 会失去“清楚该答”和“清楚该用外部动作”的参照。

## 2. 目标 TAG 契约

所有 tag 输出至少保留兼容字段：

```python
semantic_tags: dict[str, bool]
active_semantic_tags: list[str]
```

同时新增诊断字段：

```python
semantic_tag_evidence: dict[str, list[{
  "source": "question|metadata|student_reasoning|derived|candidate",
  "text": "...",
  "confidence": 0.0
}]]
```

`semantic_tags` 仍是 bool 字典，便于现有 scoring/mining 使用；`semantic_tag_evidence` 给 teacher prompt、diagnostics、测试断言使用。

### 2.1 主语义标签

| Tag | 定义 | 主要动作倾向 | 禁止误触发 |
| --- | --- | --- | --- |
| `TIME_SENSITIVE` | 正确答案依赖当前时间、近期事件、最新版本、当前职位、实时状态，或题目显式要求 latest/current/today/recent/now/as of today。 | `SEARCH`; search 不可用且无法可靠回答时可能 `REFUSE` | 固定历史时间，如 `as of 2010`；数学题；静态事实题 |
| `NEW_OR_TAIL_KNOWLEDGE` | 新出现知识、长尾实体、低频事实、多跳知识链，参数知识不稳定，外部检索可显著降低幻觉。 | `SEARCH` | 高频百科事实、常识题、纯数学题；不要仅因任意 `source` 字段触发 |
| `FALSE_PREMISE` | 问题预设了一个需要否定的事实；顺着前提回答会误导。 | 可可靠纠正时 `ANSWER` with correction；否则 `REFUSE` 或先 `SEARCH` 验证 | 仅仅无法验证、search 没结果、普通争议性问题 |
| `MISCONCEPTION_RISK` | 问题容易诱导常见误解、伪科学、民间谣言、imitative falsehood。 | corrective `ANSWER` / `SEARCH` / `REFUSE` | 仅因 `always/never/prove that/is it true` 触发 |
| `MISSING_INFO` | 缺少完成任务所必需的关键 slot；不澄清会导致错误或无意义输出。 | `CLARIFY` | 缺少非关键偏好；可以合理默认并说明的情况；事实题只是需要检索 |
| `CALCULATION_REQUIRED` | 存在具体计算、长算术链、代数/数值求解，calculator 能显著降低错误风险。 | `CALCULATE` | 所有 `boundary_type=reasoning`；所有 GSM8K/MATH；仅有数字但无需计算 |
| `JUSTIFIED_REFUSE` | 直接回答不负责任，且 search/clarify/calculate 也无法合理解决，或工具不可用导致无法 grounded。 | `REFUSE` | search 可用的动态事实；clarify 可用的缺槽；普通低置信难题 |

### 2.2 派生动作标签

新增并优先使用 action-specific derived tags：

| Tag | 派生规则 | 用途 |
| --- | --- | --- |
| `SEARCH_REQUIRED` | `TIME_SENSITIVE` 或 `NEW_OR_TAIL_KNOWLEDGE`，或 metadata 明确 external evidence needed | teacher / mining / utility 中对应 `SEARCH` |
| `CALCULATE_REQUIRED` | 可作为 `CALCULATION_REQUIRED` 的动作别名；如保留，必须与 `CALCULATION_REQUIRED` 同步 | 兼容 action-specific 命名 |
| `CLARIFY_REQUIRED` | `MISSING_INFO` 且 `example.can_clarify=True` | teacher / mining / utility 中对应 `CLARIFY` |
| `TOOL_REQUIRED` | `SEARCH_REQUIRED or (CALCULATION_REQUIRED and can_calculate) or CLARIFY_REQUIRED` | 仅 summary / diagnostics，不作为 teacher 主 tag |

`TOOL_REQUIRED` 不再表示某个具体动作，也不应单独给 `SEARCH` 加分。

## 3. Process Uncertainty 契约

process tags 描述学生生成过程的不确定性，不描述题目类型。它们的用途限定为：

- boundary mining priority；
- teacher 的辅助 context；
- calibration 分层分析；
- diagnostics。

它们不得进入 `utility_rel()` 主体，也不得单独决定 chosen/rejected。

| Tag | 目标定义 | 实现要求 |
| --- | --- | --- |
| `STRUGGLE_LONG` | 学生在动作前生成异常长 reasoning trace | 只统计 `reasoning_attempt` / `reason_prefix`，不统计 prompt、raw JSON、tool observation；优先 tokenizer token count，次选 whitespace count；阈值可先用 config，后续支持 dataset percentile |
| `HAS_SELF_REPAIR` | reasoning 中出现撤回、重新计算、修正前一步 | 移除 `however`；匹配 `wait/actually/let me revise/on second thought/correction/recalculate` 等明确修正词 |
| `LOW_LOGIT_MARGIN` | top-1 与 top-2 action 概率或候选 confidence 差距小 | 优先用 `PolicyOutput.action_probabilities/action_scores`；其次用 top-k candidate confidence margin；缺失时返回 false/unknown diagnostic，不用文本 `uncertain` |
| `HIGH_BRANCHING` | 多个动作或推理路径竞争明显 | 优先用 action probability entropy / 多候选 confidence 接近；文本正则只看明确 `alternatively/either way/option A`，不匹配普通 `or` |

## 4. 文件级迭代计划

### P0. 统一 semantic tag 实现

目标文件：

- `src/mcagent_core/features/semantic_tags.py`
- `src/mcagent_boundary/features/semantic_tags.py`
- `src/mcagent_core/scoring/action_oracle.py`
- `src/mcagent_boundary/scoring/helpfulness.py`
- `src/mcagent_boundary/scoring/utility.py`
- `src/mcagent_boundary/mining/boundary_mining.py`
- `src/mcagent_boundary/annotation/teacher_label.py`

要求：

- 只保留一套权威判定逻辑。建议放在 `mcagent_core.features.semantic_tags`，`mcagent_boundary.features.semantic_tags` 做薄 wrapper，避免 core/boundary 双规则继续漂移。
- 新增 `infer_semantic_tag_details()` 或等价函数，返回 bool tags + evidence；原 `infer_semantic_tags()` 保持兼容，只返回 bool tags。
- tag 判定默认只看 `question` 和可信 `metadata`；`reason_prefix` 只允许作为低权重 evidence，不得让模型自述把静态题变成 `TIME_SENSITIVE` 或 `CALCULATION_REQUIRED`。
- `TIME_SENSITIVE` 必须识别固定历史时间并降权，例如 `as of 2010/current through 2020` 不应触发。
- `FALSE_PREMISE` 与 `cannot verify` 分离；后者只能参与 `JUSTIFIED_REFUSE` 的 derived 判断。
- `NEW_OR_TAIL_KNOWLEDGE` 对 MintQA 可用 dataset/split/metadata 作强 evidence，但不要泛化成“任何有 source 字段都触发”。
- `CALCULATION_REQUIRED` 删除“任意 reasoning/mathematical dataset 即触发”的逻辑；math dataset 只能作弱 prior，必须还有数字关系、表达式、运算词、metadata 计算标注、或学生候选表达式 evidence。
- `TOOL_REQUIRED` 改为 derived summary；`SEARCH_REQUIRED / CLARIFY_REQUIRED` 加入输出。
- 全链路把 `SEARCH` 相关判断从 `TOOL_REQUIRED` 改成 `SEARCH_REQUIRED or TIME_SENSITIVE or NEW_OR_TAIL_KNOWLEDGE`。

### P0. Candidate canonicalization 与 validity

目标文件：

- 新增：`src/mcagent_boundary/rollout/candidate_schema.py` 或 `src/mcagent_boundary/annotation/candidate_schema.py`
- 修改：`src/mcagent_core/prompting/build_prompts.py`
- 修改：`src/mcagent_boundary/rollout/branch_actions.py`
- 修改：`src/mcagent_boundary/annotation/teacher_label.py`
- 修改：`src/mcagent_boundary/training/make_dpo_pairs.py`
- 修改：`src/mcagent_boundary/schemas/action_tools.json`

要求：

- 建立单一 canonicalization 函数，发生在 rollout 解析后、teacher 标注前、branch 构造前。
- 每个 candidate 增加：
  - `canonical_action_input`
  - `valid_candidate`
  - `schema_diagnostics`
- 标准字段：
  - `ANSWER`: `answer`
  - `SEARCH`: `query`
  - `CALCULATE`: `expression`
  - `CLARIFY`: `question`；`slot` 可选，除非实际 tool/schema 决定继续要求
  - `REFUSE`: `reason`
- 可安全映射：
  - `ANSWER`: `response/result/final/code/courses/books/routine/challenge/...` 等非标准键序列化或压缩进 `answer`
  - `SEARCH`: `search_query/question/query_text` -> `query`
  - `CALCULATE`: `expr/formula/equation/calculation` -> `expression`
  - `CLARIFY`: `missing_info/slots/slots_to_fill/missing_slot` -> 自然语言 `question`
  - `REFUSE`: 空 `reason` 可用同 candidate 的 `brief_rationale`，但必须非空
- canonicalization 后仍缺必填字段的 candidate 标记 invalid，并从以下环节剔除：
  - teacher 输入；
  - mining；
  - `utility_rel` 候选评分；
  - pair chosen/rejected。
- `build_prompts.py` 不再把空字符串补成“可训练候选”；可以最小修复字段形状，但 validity 必须显式记录。
- `action_tools.json` 与真实工具对齐：当前 `ClarifyTool` 只使用 `question`，若 `slot` 不再强制，需要同步 schema；若保留 `slot` 必填，则 canonicalization 必须无 gold 泄漏地生成 slot。

### P0. Teacher prompt 与 fallback 评分升级

目标文件：

- `src/mcagent_boundary/prompts/teacher_tag_reflect.md`
- `src/mcagent_boundary/prompts/teacher_action_recommend.md`
- `src/mcagent_boundary/annotation/teacher_label.py`

要求：

- teacher prompt 必须包含每个 valid candidate 的：
  - `rank`
  - `action`
  - `confidence`
  - `brief_rationale`
  - canonicalized `action_input`
  - `valid_candidate`
  - `schema_diagnostics`
- prompt 中写入 tag 定义摘要，至少覆盖：
  - `MISSING_INFO`: only when a critical slot is absent.
  - `FALSE_PREMISE`: only when the question presupposes a false fact.
  - `MISCONCEPTION_RISK`: common false belief / imitative falsehood risk.
  - `CALCULATION_REQUIRED`: concrete computation materially reduces error.
  - `SEARCH_REQUIRED`: external or fresh evidence is needed.
- teacher 输出 validation 改为按 `(rank, action)` 对齐，而不是只按 action 对齐。
- 每个 valid candidate 都必须有 helpfulness score；invalid candidate 不送 teacher。
- rule fallback 使用同一套 canonical action_input 与 validity，不再让空 schema 进入训练。
- fallback scoring 中：
  - `SEARCH` 高分只看 `SEARCH_REQUIRED / TIME_SENSITIVE / NEW_OR_TAIL_KNOWLEDGE`，不看 summary `TOOL_REQUIRED`。
  - `CLARIFY` 高分要求 `CLARIFY_REQUIRED` 或 critical `MISSING_INFO` 且 can clarify。
  - `REFUSE` 高分要求 `JUSTIFIED_REFUSE / FALSE_PREMISE`，或工具不可用且无法 grounded。

### P1. Process features 改成真实不确定性信号

目标文件：

- `src/mcagent_core/features/extract_process_features.py`
- `src/mcagent_boundary/features/process_features.py`
- `src/mcagent_boundary/rollout/branch_actions.py`
- `src/mcagent_core/rollout/policy.py`

要求：

- `compute_process_features()` 接收并传递：
  - `reasoning_attempt`
  - `candidate_confidences`
  - `action_probabilities`
  - `action_scores`
  - `confidence_source`
- rollout record 保存 `action_probabilities/action_scores/confidence_source`，供 diagnostics 和 calibration 使用。
- `LOW_LOGIT_MARGIN` 使用：
  - top-1/top-2 action probability margin；
  - 或 candidate confidence margin；
  - 无数值时输出 diagnostic `missing_action_margin_signal`。
- `HIGH_BRANCHING` 使用：
  - action entropy / 多候选接近；
  - 或明确分支表达；
  - 不用普通 `or`。
- `STRUGGLE_LONG` 只看 reasoning；不要把 raw JSON 或 prompt echo 算进去。

### P1. Boundary mining 与 anchor 规则收紧

目标文件：

- `src/mcagent_boundary/mining/boundary_mining.py`
- `src/mcagent_boundary/mining/anchor_sampling.py`
- `src/mcagent_boundary/scripts/02_rollout_all.py`

要求：

- mining 只处理 `valid_candidate=true` 的候选。
- `boundary_critical` 必须同时满足：
  - 至少 2 个 valid candidate；
  - 包含 `ANSWER` 和至少一个非 `ANSWER` candidate；
  - 存在真实 action 竞争：confidence/probability margin 低，或 `U_rel` 竞争，或 natural action 与 tag-supported action 冲突；
  - boundary score 达标。
- process feature 只加小权重，不能单独把样本推入 `boundary_critical`。
- `_ACTION_TAGS` 更新：
  - `SEARCH`: `SEARCH_REQUIRED`, `TIME_SENSITIVE`, `NEW_OR_TAIL_KNOWLEDGE`
  - `CALCULATE`: `CALCULATION_REQUIRED`
  - `CLARIFY`: `CLARIFY_REQUIRED`, `MISSING_INFO`
  - `REFUSE`: `JUSTIFIED_REFUSE`, `FALSE_PREMISE`
- `clear_answer_anchor` 条件：
  - stable process；
  - high confidence `ANSWER`；
  - 无 action-specific external pressure；
  - valid ANSWER action_input 非空。
- `clear_external_anchor` 条件：
  - stable process；
  - high confidence non-ANSWER；
  - 该 action 有对应 action-specific tag support；
  - action_input valid。
- mining summary 必须输出：
  - `num_valid_candidates`
  - `invalid_candidate_rate`
  - `empty_action_input_count`
  - `pool_distribution`
  - `tag_distribution`
  - `boundary_reason_distribution`
  - `clear_answer_anchor_count`
  - `clear_external_anchor_count`

### P1. Utility / calibration 约束

目标文件：

- `src/mcagent_boundary/scoring/utility.py`
- `src/mcagent_boundary/scoring/helpfulness.py`
- `src/mcagent_boundary/scripts/0x_calibrate_relative_utility.py`
- `src/mcagent_boundary/evaluation/*`

要求：

- `utility_rel()` 继续禁止 process features。
- `semantic_bonus` 只给 action-specific tag：
  - `SEARCH_REQUIRED_SEARCH`
  - `TIME_SENSITIVE_SEARCH`
  - `NEW_OR_TAIL_KNOWLEDGE_SEARCH`
  - `CALCULATION_REQUIRED_CALCULATE`
  - `CLARIFY_REQUIRED_CLARIFY`
  - `FALSE_PREMISE_REFUSE`
  - `JUSTIFIED_REFUSE_REFUSE`
- 每个 bonus 仍保持小权重，单项不超过 0.05，单 candidate cap 不超过 0.15。
- `outcome_helpfulness()` 中：
  - `SEARCH` 不再依赖 `TOOL_REQUIRED`；
  - `REFUSE` 不再把 `TOOL_REQUIRED and not can_search` 当作唯一泛化规则，改看 `SEARCH_REQUIRED` 且 search unavailable 且 direct answer incorrect/unavailable。
- calibration 必须使用 eval-style rollout，即执行工具 + finalize 后得到的 `U_real`；如果输入是 annotation rollout，需要明确报错或标记不可校准。
- calibration summary 必须报告：
  - Spearman；
  - Kendall；
  - argmax hit；
  - per-action AUC；
  - per-action 样本量；
  - empty/invalid candidate 过滤计数。

### P2. Rollout 并行、mixing、progress

这些保持原 v0.2-fix 方向，但排在 tag/schema 修复之后。

#### 并行 rollout

目标文件：

- `src/mcagent_boundary/rollout/generate_rollouts.py`
- `src/mcagent_boundary/scripts/02_rollout_all.py`
- `src/mcagent_boundary/configs/rollout.yaml`

要求：

- 新增多进程按 dataset 并行模式：
  - `parallel_datasets: 4`
  - CLI: `--datasets gsm8k,math,in3,mintqa`
  - CLI: `--parallel-datasets N`
  - CLI: `--resume`
  - CLI: `--merge-only`
- 每个 worker 写独立 shard：
  - `rollouts/by_dataset/{dataset}.jsonl`
- parent 按 config 顺序 merge 成 `all_rollouts.jsonl`，再做 mining。
- 不做 GPU 自动检测；并发数由 config/CLI 显式控制。

#### 训练集 mixing

目标文件：

- 新增：`src/mcagent_boundary/training/mix_pairs.py`
- 新增：`src/mcagent_boundary/scripts/05_mix_pairs.py` 或插入 `04_make_pairs.py` 后置步骤
- 修改：`src/mcagent_boundary/scripts/06_train_dpo.py`

要求：

- make-pairs 后、train-dpo 前新增 mixing。
- 默认数据集均衡，支持显式权重：

```yaml
pair_mixing:
  strategy: balanced_by_dataset
  weights:
    gsm8k: 1.0
    math: 1.0
    in3: 1.0
    mintqa: 1.0
  seed: 42
```

- 输出：
  - `mixed_train_step_dpo_pairs.jsonl`
  - `mixed_eval_step_dpo_pairs.jsonl`
  - `mix_summary.json`
- summary 包含 dataset/split/pool/boundary_type/action-pair/delta_u_rel 分布，以及 shortfall/dropped 统计。
- `06_train_dpo.py` 新增 `--train-pairs` / `--eval-pairs`，默认优先读取 mixed pair 文件，不存在才回退原 pair 文件。

#### progress

目标文件：

- `scripts/02_rollout_all.py`
- `scripts/03_teacher_label_boundary.py`
- `scripts/04_make_pairs.py`
- mixing 脚本

要求：

- rollout worker 内显示 dataset-level tqdm。
- parent 显示 dataset shard 完成进度。
- teacher 标注显示 success/fallback/failure 计数。
- pair/mixing 显示 produced/dropped/diagnostics。
- 所有脚本支持 `--no-progress`。

## 5. Pair 构造硬约束

`training/make_dpo_pairs.py` 必须新增以下过滤：

- selected record 有 unresolved invalid rollout issue 时跳过。
- chosen/rejected 都必须：
  - `valid_candidate=true`
  - `action_input` 标准字段非空
  - `is_debug_fallback=false`
  - `is_student_candidate=true`
- chosen/rejected 必须不同 action type。
- `delta_u_rel >= min_utility_gap`。
- teacher label 若 `rubric_degenerate=true`，跳过。
- diagnostics 新增：
  - `invalid_action_input_after_canonicalization`
  - `dropped_invalid_candidate`
  - `not_enough_valid_candidates`
  - `teacher_missing_candidate_score`
  - `no_valid_pair_after_schema_filter`

输出 pair metadata 新增：

```json
{
  "chosen_valid_candidate": true,
  "rejected_valid_candidate": true,
  "chosen_schema_diagnostics": [],
  "rejected_schema_diagnostics": [],
  "active_semantic_tags": [],
  "semantic_tag_evidence": {},
  "process_features": {}
}
```

## 6. 测试计划

### Unit tests

新增或补齐 tests，覆盖：

- semantic tag fixtures：
  - latest/current/today -> `TIME_SENSITIVE`
  - `as of 2010` 固定历史 -> 不触发 `TIME_SENSITIVE`
  - MintQA long-tail/new metadata -> `NEW_OR_TAIL_KNOWLEDGE`
  - false premise -> `FALSE_PREMISE`
  - cannot verify only -> 不触发 `FALSE_PREMISE`
  - TruthfulQA 风格常见误解 -> `MISCONCEPTION_RISK`
  - `prove that` 数学题 -> 不触发 `MISCONCEPTION_RISK`
  - IN3 critical missing slot -> `MISSING_INFO` 和 `CLARIFY_REQUIRED`
  - minor preference missing -> 不触发 `MISSING_INFO`
  - explicit arithmetic / expression -> `CALCULATION_REQUIRED`
  - reasoning dataset with no concrete calculation -> 不触发 `CALCULATION_REQUIRED`
- process feature fixtures：
  - raw JSON 包含 `uncertain` 不触发 `LOW_LOGIT_MARGIN`
  - action probabilities margin 小触发 `LOW_LOGIT_MARGIN`
  - 普通 `or` 不触发 `HIGH_BRANCHING`
  - `however` 不触发 `HAS_SELF_REPAIR`
  - `wait, actually...` 触发 `HAS_SELF_REPAIR`
- candidate schema fixtures：
  - ANSWER 非标准键可规范化到 `answer`
  - SEARCH `search_query/query_text` 可映射
  - CALCULATE 无表达式则 invalid
  - CLARIFY `slots_to_fill/missing_info` 生成 question
  - REFUSE 空 reason 用 brief_rationale 填充；仍空则 invalid
- teacher payload fixtures：
  - prompt candidate 包含 action_input / valid_candidate / schema_diagnostics / tag evidence
  - validator 按 `(rank, action)` 对齐 helpfulness
  - invalid candidate 不送 teacher
- mining fixtures：
  - process-only 不足以进入 `boundary_critical`
  - clear answer anchor 可产生
  - clear external anchor 可产生
- pair builder fixtures：
  - 空必填字段不能进入 chosen/rejected
  - teacher degenerate 跳过
  - schema invalid candidate 跳过
  - diagnostics reason 正确
- mixing fixtures：
  - 数据集均衡
  - 显式权重
  - 数据集不足 shortfall
  - seed 可复现

### Smoke tests

建议命令：

```bash
PYTHONPATH=src python3 -m compileall src/mcagent_boundary src/mcagent_core
PYTHONPATH=src python3 src/mcagent_boundary/scripts/02_rollout_all.py --limit-per-dataset 2 --output-dir /tmp/boundary_smoke_rollouts
PYTHONPATH=src python3 src/mcagent_boundary/scripts/03_teacher_label_boundary.py --input-dir /tmp/boundary_smoke_rollouts --output-dir /tmp/boundary_smoke_rollouts
PYTHONPATH=src python3 src/mcagent_boundary/scripts/04_make_pairs.py --input-dir /tmp/boundary_smoke_rollouts --output-dir /tmp/boundary_smoke_rollouts
```

如果使用 HF student 主模型，设置 `STUDENT_MODEL_PATH`；如果 fallback 到 heuristic，只能视为 smoke，不能产出正式训练 pair。

### Acceptance gates

正式 DPO 前必须满足：

- `chosen/rejected` 中 empty standard action_input count = 0。
- `chosen/rejected` 中 invalid candidate count = 0。
- teacher 对每个 valid candidate 的 helpfulness 覆盖率 = 100%。
- mining summary 中 `clear_answer_anchor_count > 0` 且 `clear_external_anchor_count > 0`；若代表性 full run 为 0，阻止正式训练。
- `boundary_critical` 不能由 process feature 单独触发；diagnostics 可追溯每个 pool reason。
- calibration 达标：
  - Spearman >= 0.5
  - argmax hit >= 0.6
  - 非空 per-action AUC >= 0.6
- `utility_rel()` 代码路径没有读取 process feature。
- student prompt / training pair 不泄漏 `dataset/boundary_type/gold_answer/gold_clarify_reply` 到 action_input。

## 7. 不变量

- Gold 只能用于 offline scoring / eval / calibration，不能注入 student candidate 或 DPO completion。
- annotation 阶段不执行工具，不假造 observation。
- eval/calibration 阶段才执行工具并 finalize，产生 `U_real`。
- `HeuristicPolicy` 只能 smoke/debug，不能作为 mainline training rollout。
- `TOOL_REQUIRED` 只能是 summary，不再作为 teacher、utility、mining 的主证据。
- process features 是 uncertainty signal，不是 semantic truth；不能把“模型说不确定”当作真实低 logit margin。
